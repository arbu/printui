from setuptools import setup

# This file exists only for backward compatibility.
# All configuration is stored in pyproject.toml,
# but some tools may still require the existence of setup.py.
setup()
